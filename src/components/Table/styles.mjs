export const Table = `
  box-sizing: border-box;
  display: block;

  width: clamp(calc(375px - 32px), 100%, 90vh);
  max-height: 75%;

  padding: 1rem;
  margin: 3rem auto; 

  font-size: 1.25rem; 
  overflow-y: auto;
`
